import swaggerJsdoc from 'swagger-jsdoc';

const options: swaggerJsdoc.Options = {
  definition: {
    openapi: '3.0.0',
    info: {
      title: 'Nanaya',
      version: '1.0.0',
    },
    components: {
      securitySchemes: {
        bearerAuth: {
          type: 'http',
          scheme: 'bearer',
          bearerFormat: 'JWT',
          description: 'Enter your JWT token from /users/login',
        },
      },
    },
  },
  apis: ['./src/routes/*.ts'], // picks up your JSDoc annotations
};

export const swaggerSpec = swaggerJsdoc(options);